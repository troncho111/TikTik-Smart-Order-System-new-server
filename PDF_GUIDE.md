# 🚀 מדריך לעיצוב PDF - TikTik Smart Order System

## 🎯 מטרה

מדריך זה מסביר איך לשנות את העיצוב של טופס ההזמנה ב-PDF בקלות, גם ללא ידע בתכנות.

## 📂 מבנה הקבצים

כל מה שקשור ל-PDF נמצא ב-3 מקומות:

1.  **תבנית HTML:**
    -   **נתיב:** `templates_new/order_template.html`
    -   **מה זה?** השלד של המסמך. הוא קובע **מה** יוצג (למשל, "כאן תהיה תמונת מלון").

2.  **קובץ עיצוב CSS:**
    -   **נתיב:** `static/css/pdf_style.css`
    -   **מה זה?** הלב של העיצוב. הוא קובע **איך** כל דבר ייראה (צבעים, גדלים, גופנים).

3.  **קוד לוגיקה:**
    -   **נתיב:** `services_new/pdf_service.py`
    -   **מה זה?** המוח מאחורי הקלעים. הוא מכין את הנתונים ושולח אותם לתבנית.
    -   **⚠️ אין צורך לגעת בקובץ הזה!**

---

## 🎨 איך לשנות את העיצוב?

**כל השינויים נעשים בקובץ אחד בלבד:** `static/css/pdf_style.css`

### 1. שינוי צבעים ראשיים

פתח את `pdf_style.css` וחפש את החלק הבא (שורות 40-60):

```css
:root {
  /* 🎨 צבעים ראשיים - שנה כאן! */
  --brand-blue: #1e6fff;           /* כחול ראשי */
  --brand-blue-dark: #0846b8;      /* כחול כהה */
  --brand-red: #d63031;            /* אדום ראשי */
  --brand-red-dark: #b71c1c;       /* אדום כהה */
  
  /* צבעי רקע וגבולות */
  --brand-bg: #f8f9fb;             /* רקע אפור בהיר */
  --white: #ffffff;                /* לבן */
  --border: #e1e4eb;               /* גבול אפור */
}
```

-   **רוצה לשנות את הכחול?** החלף את `#1e6fff` בצבע חדש.
-   **רוצה לשנות את האדום?** החלף את `#d63031` בצבע חדש.

### 2. שינוי גודל טקסט

הקובץ מחולק לסקציות עם הערות ברורות בעברית.

**דוגמה: הגדלת שם הלקוח בעמוד השער**

1.  חפש את הסקציה `1️⃣ עמוד שער (COVER PAGE)` (שורה 85).
2.  מצא את הכלל `.cover-customer` (שורה 115).
3.  שנה את `font-size`:

```css
.cover-customer,
.cover-line1 {
  font-size: 56px;              /* 👈 שנה כאן לגודל הרצוי */
  font-weight: 700;
  /* ... */
}
```

### 3. שינוי גודל לוגו

1.  חפש את הסקציה `2️⃣ באנר עליון (BANNER)` (שורה 175).
2.  מצא את הכלל `.logo` (שורה 215).
3.  שנה את `width` ו-`height`:

```css
.logo {
  width: 56px;                  /* 👈 רוחב הלוגו */
  height: 56px;                 /* 👈 גובה הלוגו */
  /* ... */
}
```

### 4. שינוי עיגול פינות

1.  חפש את הסקציה `5️⃣ כרטיסים (CARDS)` (שורה 350).
2.  מצא את הכלל `.card` (שורה 355).
3.  שנה את `border-radius`:

```css
.card {
  border: 2px solid var(--border);
  border-radius: 16px;          /* 👈 שנה כאן (למשל, ל-8px) */
  /* ... */
}
```

---

## 📦 מבנה הבלוקים בתבנית

התבנית `order_template.html` בנויה מבלוקים חכמים. כל בלוק מוצג רק אם יש לו נתונים.

| בלוק                  | משתנה תנאי (ב-HTML)                               | מתי מוצג?                                      |
| --------------------- | -------------------------------------------------- | ---------------------------------------------- |
| **אירוע**             | `{% if event_name %}`                              | אם יש שם אירוע                                 |
| - תגי קבוצות          | `{% if home_team_badge or away_team_badge %}`      | אם יש תג לקבוצה ביתית או אורחת                 |
| - מפת מקומות          | `{% if seatmap_image or stadium_image %}`          | אם יש תמונת מפה או אצטדיון                    |
| **מלון**              | `{% if hotel_name %}`                              | אם יש שם מלון                                  |
| - תמונות מלון         | `{% if hotel_image_path or hotel_image %}`         | אם יש תמונת מלון                               |
| - פרטים נוספים       | `{% if hotel_address or hotel_website %}`          | אם יש כתובת או אתר למלון                      |
| **טיסות**             | `{% if flights and flights|length > 0 %}`          | אם יש רשימת טיסות (והיא לא ריקה)               |
| **נוסעים**            | `{% if passengers and passengers|length > 0 %}`    | אם יש רשימת נוסעים (והיא לא ריקה)              |
| **תנאים**              | `{% if legal_text or terms_text %}`                | אם יש טקסט תנאים                               |
| - מדריך הושבה         | `{% if seating_guide_image or seating_images %}`   | אם יש תמונת מדריך הושבה או רשימת תמונות       |

**זה אומר שאתה לא צריך לדאוג להסתיר בלוקים - המערכת עושה את זה אוטומטית!**

---

## 💡 סיכום

-   **רוצה לשנות עיצוב?** ערוך את `static/css/pdf_style.css`.
-   **רוצה לשנות תוכן?** ערוך את `templates_new/order_template.html`.
-   **צריך עזרה?** כל הקוד מתועד בעברית.

בהצלחה! 🚀
